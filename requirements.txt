# DRAI V1 - Minimum Requirements
# Dynamic Resonance AI: Internal State for Transformers

# Core dependencies
torch>=2.0.0
transformers>=4.30.0

# Optional: For development and testing
pytest>=7.0.0           # Running tests
jupyter>=1.0.0          # Running notebooks
matplotlib>=3.5.0       # Visualization
numpy>=1.21.0           # Numerical operations

# Note: CPU-only installation is sufficient for testing
# For faster inference, install with CUDA support:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
